const Footer = () => {
  return <footer className={styles.footer}>Website created by JJ&copy; using TMDB Api</footer>;
};

const styles = {
  footer: "bg-slate-600 h-8 text-white flex items-center justify-center w-full mt-8",
};

export default Footer;
